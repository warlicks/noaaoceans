# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
os:
  - linux
  - osx
sudo: false
cache: packages

matrix:
  include:
    - r: release
    - r: oldrel
    - r: devel

after_success:
  - Rscript -e 'covr::codecov()'
